#ifndef ARRAY_H 
#define ARRAY_H

#include <stdio.h> 
#include <stdlib.h>
#include <string.h>

int generateArray(char* filename, int** array, int* size);
void printArray(int* array, int size);

#endif
